export const metadata = {
  title: "For Business",
  description:
    "Cash, chat, commerce — one platform for COD-heavy, WhatsApp-first commerce.",
};

export default function ForBusinessLayout({ children }) {
  return children;
}
